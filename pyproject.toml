[project]
name = "local-rag"
version = "0.1.0"
description = "Retrieval Augmented Generation running on a local model"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv>=1.2.2",
    "scikit-learn>=1.7.2",
    "sentence-transformers>=5.3.0",
]
